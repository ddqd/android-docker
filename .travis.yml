sudo: required

services:
  - docker

before_install:
  - docker build -t android-docker .
  - docker run -d android-docker
  - docker ps -a

script:
  - docker run android-docker /bin/sh -c "chmod +x /usr/local/android-sdk/tools/android && /usr/local/android-sdk/tools/android list"
